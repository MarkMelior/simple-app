/**
 * Удаляет всё, кроме букв латиницы / кириллицы и пробелов.
 * — [^a-zA-Zа-яА-Я\s] — «любой символ, который НЕ буква или пробел»
 */
export const cleanText = (text: string) => text.replace(/[^a-zA-Zа-яА-Я\s]/g, '');
